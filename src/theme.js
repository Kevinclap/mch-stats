import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#323232'
    },
    secondary: {
      main: '#c99f3f'
    },
    text: {
      main: '#FFFFFF'
    },
    background: {
      main: '#212121'
    }
  }
});

export default theme;
